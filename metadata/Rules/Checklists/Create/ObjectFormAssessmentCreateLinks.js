import { ChecklistLibrary as libChecklist } from '../ChecklistLibrary';

export default function objectFormAssessmentCreateLinks(pageClientAPI) {

    return libChecklist.objectFormAssessmentCreateLinks(pageClientAPI);
}
