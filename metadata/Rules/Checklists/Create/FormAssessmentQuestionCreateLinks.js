import { ChecklistLibrary as libChecklist } from '../ChecklistLibrary';

export default function formAssessmentQuestionCreateLinks(pageClientAPI) {

    return libChecklist.formAssessmentQuestionCreateLinks(pageClientAPI);
}
