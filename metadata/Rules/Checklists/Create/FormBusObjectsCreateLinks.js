import { ChecklistLibrary as libChecklist } from '../ChecklistLibrary';

export default function formBusObjectCreateLinks(pageClientAPI) {

    return libChecklist.formBusObjectCreateLinks(pageClientAPI);
}
