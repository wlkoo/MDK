import { ChecklistLibrary as libChecklist } from '../ChecklistLibrary';

export default function formAssessmentQuestionProcessLoop(pageClientAPI) {

    return libChecklist.formAssessmentQuestionProcessLoop(pageClientAPI);
}
