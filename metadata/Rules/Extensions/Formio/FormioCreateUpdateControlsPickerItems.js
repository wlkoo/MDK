import {FormioEventLibrary as LibFormioEvent} from './FormioLibrary';

export default function FormioCreateUpdateControlsPickerItems(controlProxy) {
    return LibFormioEvent.createUpdateControlsPickerItems(controlProxy);
}
